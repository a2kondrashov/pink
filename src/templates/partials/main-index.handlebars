<div class="main">
	<!-- Intro -->
	{{>intro-index}}
	<!-- Info -->
	{{>info}}
	<!-- Promo -->
	{{>promo-index}}
	<!-- Features -->
	{{>features}}
	<!-- Reviews-->
	{{>reviews}}
	<!-- Price plan -->
	{{>price-plan}}
	<!-- Contacts -->
	{{>contacts}}
	<!-- Map -->
	<div class="map" id="map">
	</div>
</div>
