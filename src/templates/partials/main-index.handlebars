<div class="main">
	<!-- Intro -->
	{{>intro-index}}
	<!-- Info -->
	{{>info}}
	<!-- Promo -->
	{{>promo-index}}
	<!-- Features -->
	{{>features}}
	<!-- Mega feature -->
	{{>mega-feature}}
	<!-- Reviews-->
	{{>reviews}}
	<!-- Price plan -->
	{{>price-plan}}
	<!-- Contacts -->
	{{>contacts}}
	<!-- Map -->
	<div class="map" id="map">
	</div>
</div>
